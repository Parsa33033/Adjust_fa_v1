

class Message {
  String sender;
  String receiver;
  String message;
  int senderId;
  int receiverId;

  Message(
      this.sender, this.receiver, this.message, this.senderId, this.receiverId);
}
